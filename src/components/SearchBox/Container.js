import styled from 'styled-components';

import { colors } from 'variables';

const Container = styled.div`
  background-color: ${colors.GREEN_SEA};
  padding: 10px 15px;
`;

export default Container;
